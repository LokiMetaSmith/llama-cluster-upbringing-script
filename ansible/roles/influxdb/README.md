# InfluxDB Role

This role deploys InfluxDB v2 via Nomad for highly queryable time-series storage.

It exposes port 8086 by default. Configuration includes initialization credentials and bucket details.
