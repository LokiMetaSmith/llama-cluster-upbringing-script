data_dir  = "{{ nomad_data_dir }}"
bind_addr = "{{ advertise_ip }}"

advertise {
  http = "{{ advertise_ip }}"
  rpc  = "{{ advertise_ip }}"
  serf = "{{ advertise_ip }}"
}

server {
  enabled = true
  bootstrap_expect = {{ nomad_bootstrap_expect }}
}

client {
  enabled = true
}
