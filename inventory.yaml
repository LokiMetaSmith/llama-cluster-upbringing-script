all:
  children:
    controller_nodes:
      hosts:
        localhost:
          ansible_connection: local
          ansible_host: 127.0.0.1
    worker_nodes:
      hosts:
        localhost:
          ansible_connection: local
          ansible_host: 127.0.0.1
        AID-E-23:
          ansible_host: AID-E-23
  vars:
    ansible_user: user
