all:
  children:
    controller_nodes:
      hosts:
        AID-E-24:
        localhost:
          ansible_host: 127.0.0.1
    worker_nodes:
      hosts:
        AID-E-24:
        localhost:
          ansible_host: 127.0.0.1
  vars:
    ansible_user: user
