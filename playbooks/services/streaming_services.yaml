- hosts: all
  become: yes
  vars_files:
    - "{{ playbook_dir }}/../group_vars/all.yaml"
  roles:
    - { role: sunshine, tags: ['sunshine', 'streaming'] , when: "node_tier == 'high'" }
    - { role: magic_mirror, tags: ['magic_mirror', 'streaming'] , when: "node_tier == 'high'" }
